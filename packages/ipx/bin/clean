#!/usr/bin/env node

const Config = require('config')
const { IPX } = require('..')

async function main () {
  const ipx = new IPX(Config.get('ipx'))
  await ipx.cleanCache()
}

main().catch(console.error)
